var overallApp = React.createClass({
    render: function(){
        return (
            <div>
            </div>
        )
    }
});

ReactDOM.render(<overallApp />, document.getElementById('tradeDesc'));

